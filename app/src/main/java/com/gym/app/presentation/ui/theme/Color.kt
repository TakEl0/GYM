/**
 * @file Color.kt
 * @brief Paleta de colores del tema oscuro azulado de la aplicación GYM.
 * Define todos los colores de la marca con tonos azules profundos, evitando
 * tonos claros o pastel para mantener una estética profesional y sobria.
 */
package com.gym.app.presentation.ui.theme

import androidx.compose.ui.graphics.Color

// --- Colores base del tema oscuro ---

/** Fondo principal de la aplicación (azul noche casi negro). */
val FondoOscuro = Color(0xFF0A0F1E)

/** Superficie principal de tarjetas y paneles. */
val SuperficieOscura = Color(0xFF121A2E)

/** Superficie elevada (menús, diálogos, tarjetas destacadas). */
val SuperficieElevada = Color(0xFF16213C)

/** Azul primario de la marca (vibrante pero profundo). */
val AzulPrimario = Color(0xFF3D7BFF)

/** Azul secundario de acento. */
val AzulSecundario = Color(0xFF5B8FFF)

/** Cian de acento para destacados y progreso. */
val CianAcento = Color(0xFF2DD4BF)

/** Texto primario (blanco azulado). */
val BlancoAzulado = Color(0xFFE8EEF9)

/** Texto secundario (gris azulado). */
val GrisAzulado = Color(0xFF8A99B8)

/** Borde sutil de superficies. */
val BordeAzulado = Color(0xFF233354)

/** Fondo de los campos de texto. */
val CampoAzulado = Color(0xFF0E1526)

/** Error (rojo atenuado para mantener el modo oscuro equilibrado). */
val ErrorOscuro = Color(0xFFFF6B6B)